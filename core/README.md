# Sheratan Core

This package contains the orchestrator, agents, memory, relay adapters and interfaces.

- Keep business logic in /core; keep UI in /ui.
- Contracts live in /interfaces and are shared.
- Big artifacts are released via GitHub Releases.
